{
  "scripts": {
    "start": "docusaurus-start",
    "build": "./syncDocs.js && docusaurus-build",
    "publish-gh-pages": "./syncDocs.js && docusaurus-publish",
    "write-translations": "docusaurus-write-translations",
    "version": "docusaurus-version",
    "rename-version": "docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.12.0"
  },
  "dependencies": {
    "react": "^16.8.6"
  }
}
